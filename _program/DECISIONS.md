# DECISIONS — append-only log

Every non-trivial decision, who/what made it, and why. Token/component changes are Opus-only and must be logged here.

---

### 2026-06-03 · Design contract locked
**Decision:** The VegaReady visual system is locked — warm grounds (Ivory-Grey for card UI, Almanac for reporting prose), Cream cards, stronger borders, paper grain on; Playfair (headlines) / Fraunces (body) / Lora (numerals) / IBM Plex Mono (labels); **muted Main** (Muted Petrol) for structure + large bands vs **saturated Accent** (Terracotta) for buttons/marks/prominent figures; status set Realized=green / Anticipated=amber / Latent=warm-grey (never red) / Alert=reserved red.
**By:** Owner, via the live theme composer (`VegaReady-Themeable-Demo.html`).
**Why:** Converged taste over three visual rounds; the muted-main/accent rule prevents large colour fills from competing with text. See `DESIGN-CONTRACT.md`.

### 2026-06-03 · Colour role rule (Main ≠ Button)
**Decision:** Structural colour over large areas must be muted; saturated colour is reserved for small attention surfaces (buttons, marks, single prominent figures), chosen to match section tone. Dense table figures stay ink.
**By:** Owner. **Why:** Bright colour at scale distracts and mutes surrounding text.

### 2026-06-03 · Preview-first delivery workflow
**Decision:** Changes are reviewed in **preview** (local) before anything is promoted to live (vegaready.com). Deploy to production only on owner approval of a phase/chunk. Deploy execution is manual (owner runs cmd from Opus's command blocks); GitHub Actions → Cloudflare automation remains an open option for later.
**By:** Owner. **Why:** Large structural + content revisions in flight; protect the live showcase.

### 2026-06-03 · Roadmap scope
**Decision:** ROADMAP.json (this directory) is the immutable scope — the audit Top-20 sequenced into 7 dependency-resolved phases. The stakeholder-grade HTML "Build Roadmap" app from the roadmap prompt is **deferred**; if built later it is generated from the same data so it can't drift.
**By:** Opus (recommendation) + owner go-ahead. **Why:** Get to building; keep one source of truth.

### 2026-06-03 · Dark-primary direction + approved dark set v1
**Decision:** The site theme direction is **dark-primary** (refined blue/green-black canvases — the JCJ system's own sanctioned dark register), with the refined warm-light system as the alternate pending its own trial. Nine dark combinations approved across two trial rounds (see `APPROVED-THEMES.md`, organized by canvas family). Taste laws codified: warm ink on cool desaturated ground; soft-teal/green mains (Verdigris leading); gold-metal accents only; the Saturation-Footprint Law (bright colour budget shrinks with saturation, including type size); the Tonal Ladder Law (same-family mains at ≥4.5:1 rung separation); derived elevation with no in-flow shadows.
**By:** Owner, via the dark trials + Theme Workbench. **Why:** Side-by-side evidence on the real dense page; dark reads more focused/alive for data-heavy intelligence. Supersedes the light-primary lock of earlier 2026-06-03 (that system remains the light alternate).

### 2026-06-03 · Reality model
**Decision:** Realized / Anticipated / Latent reality badges on every claim + a global reality filter; market-priced vs unpriced as the trade lens. Causal-edge Tracked/Proposed (observed vs reasoned) is a separate axis from event reality.
**By:** Owner-approved. **Why:** The site was mixing realized fact with hypothetical risk; this separates them, which is the platform's core honesty discipline.

### 2026-06-04 · Reserve colours validated (trial verdicts locked)
**Decision:** The COLOR-RESERVE register was validated in-context on the approved themes (`_program/artifacts/VegaReady-Reserve-Trial.html`, 13 exhibits, owner verdicts). **Adopted:** ETF Yellow `#FFD89A` as the benchmark/overlay line + "NOW" marker (charcoal text only, type-size law applies); Soft Alert Red `#D16D6D` as the fourth status register ("Elevated", between Anticipated and Alert); Royal Blue `#4169E1` for hero-stat numerals (numerals only, never background); Slate Blue VR `#8B7FD9` as chart series 3/4 with Steel Orchid VR `#9D8CC4` as the annotation layer; the risk-pacing spine (Terracotta candour → Sage Green `#B5C9A9` decompression → neutral structure); the GCC gate (Terracotta over Coral on GCC-facing surfaces); Warm Clay `#DAA06D` narrative-pivot panels; Dusty Plum `#9B6A6C` retrospective accents; Pale Mint soft-CTA zone → Fog Purple exit footer (page-end only, low-alpha tints); and the **Gold ✕ Crimson quarantine as a component law** (no gold inside alert/disclosure components). **Rejected:** the Sky Blue + Dark Teal CTA ladder (E3) — light text on Dark Teal `#0A7D7D` fails visually ("contrast for white text is bad"); the theme main remains the sole CTA register; any future commit-register attempt must re-trial with a darker fill or ink-on-light approach. Mint `#98FF98` and Electric Purple `#8A2BE2` rejections confirmed by judged exhibit (Saturation-Footprint violations).
**By:** Owner, via the Reserve Trial verdict export. **Why:** Every reserve adoption is now grounded in an in-context visual judgment on the real themes, with measured contrast — and every rejection is a written "how not to use" rule, not an assumption.

### 2026-06-04 · Light alternate locked — "Soft White Desk" (L1)
**Decision:** The light theme is locked from Light Trial v3 ("much better results — lock these in"). Full token set in `APPROVED-THEMES.md → The Light Alternate`. Core grammar: Soft White `#DDE2E6` canvas (Cool Paper/Modal siblings) · white cards + slate hairlines · cool-black ink `#2B2F34` · **headers = ink at weight** (Playfair semibold/bold, sentence case; labels 11px bold caps in ink — never thin, never colored by default; Teal Blue `#367588` sanctioned for section heads; Navy only ≥24px bold) · Navy title-zone panel with white text + gold kicker · status chips = same-hue 16% tint + darkened same-hue text · charts = Deep Teal main + slate-dash benchmark (theme-conditional benchmark token: dark→ETF Yellow, light→slate) + Light Cyan range band · actions = Teal Blue primary (hover −15%) / slate solid secondary / ink text-link (no outline buttons, no pastels, one primary per view). Six light-theme bans locked (blue ink below the 16px-bold gate; gold chart lines on light; white text on pale fills; both status-mark extremes; pastel/second-teal buttons; warm-paper default canvases).
**Process note:** v1 (dark-inversion) and v2 (first corpus pass) both failed on judged verdicts; the v3 spec came from a 5-agent diagnostic over 7 brand documents (incl. 4 newly uploaded) that converted every owner complaint into a corpus citation — all four v2 failures were corpus violations ("Cobalt… acceptable only when bold and >16pt"; "Title Zone = Navy [fill]"; gold-family "not compatible with Slate or Cobalt transitions — triggers too much contrast"; pale fills "dark/black text only"). Spec was owner-approved before the build.
**By:** Owner, via Light Trial v3. **Why:** The light alternate now shares the dark system's discipline — hue-neutral text that survives accent rotation, semantic colour at gated sizes, and judged bans instead of assumptions.

### 2026-06-04 · Typography & Emphasis system locked (all 10 exhibits approved)
**Decision:** The typography and emphasis layer is locked for BOTH palettes (full register in `APPROVED-THEMES.md → Typography & Emphasis`). Core: corpus master type scale mapped to Playfair/Fraunces/Lora (H1 28/700, H2 20/600, body 15.5 @1.5 light/**1.6 dark** — line-height is contrast-graded, word caps per role, hero stat 48–56px once per page); **the Weight Law** ("weight rises as contrast or chroma falls"; any coloured text = bold; 3-step readability ladder); italics confined to quotes/sublines/legal with the verbatim pull-quote construction and the **quote≁CTA exclusion law**; the pairwise rhythm ladder (36/28/24/44); left-aligned reasoning; caps only at 11px bold micro-labels @+.05em; links ≥14px with mandatory −15% hover; the key-stat underline accent (Royal Blue light / gold dark, ≤5%); and the **measured emphasis budget** — CTA/action layer 1–5% of painted page area with the 5-layer envelope, blank-space law (≥3× cap-height around CTAs, canvas ≥40% visible), context budgets per surface (data/risk/passive-CTA/premium/trust), and the no-CTA-inside-risk-content law. Emphasis device hierarchy locked (bold → size → ink → semantic colour → fill containers → underline) with the stacking bans.
**Process:** 3-agent research over 6 corpus documents → written spec (owner-approved before build) → 10-exhibit trial with live coverage meters → all approved ("excellent results!!").
**By:** Owner, via the Type & Emphasis Trial. **Why:** Hierarchy is now made with size/weight/spacing under numeric budgets — colour is reserved for meaning, and emphasis spend is measurable instead of felt.

### 2026-06-05 · Components & page layouts locked (Trial A verdicts)
**Decision:** The component registry is locked for both palettes (full register in `APPROVED-THEMES.md → Components & Page Layouts`). Approved whole: the 7-template layout-archetype registry + 24-ID page archetype library (A1); the four-option navigation registry with canvas-chrome/main-indicator as working default (A4 — net-new domain); the role-assigned hero set — title-zone default, Navy full-bleed front-door-only with CTA-on-white-panel, gradient section dividers, full-bleed stat (A5); the role-assigned footer set — plain meta default, CTA bar end-zones only, legal register for compliance (A6); the page-rhythm laws — buffer ranking, dense/light alternation, risk-arc-in-2–3-sections, no-CTA-in-risk (A8). Conditional locks: **cards** — light hairline + dark borderless-tint defaults, framed main-tint as highlight role; **strong-lift ("too much pop") and surround-block ("looks strange") REJECTED** — owner verdict overrides the corpus' showcase device (A2). **Tables** — current zebra default + Payne's-class dense/legal register only; row-hairlines and header-fill bands REJECTED (A3). **Forms** — approved with one construction law: text in fields/chips/buttons/tooltips is vertically centered, never top-aligned (A7).
**Process:** 3-agent research over 6 documents → Components-Charts spec (owner-approved) → 8-exhibit trial, every option rendered on both palettes → verdict export.
**By:** Owner, via Components Trial A. **Why:** One verdict per component now locks both themes simultaneously; corpus devices that fail owner taste are struck with a written reason, not silently dropped. Cycle B (charts/data-viz/iconography) finalizes next on top of this registry.

### 2026-06-05 · Charts, data-viz & iconography locked (Trial B + B2 verdicts) — design system COMPLETE
**Decision:** The fifth and final system layer is locked (full register in `APPROVED-THEMES.md → Charts, Data-Viz & Iconography`). Spec was corpus research + 3-agent **web research** (Datawrapper/Urban/Economist/FT/Tufte/Heer-Robertson/NN-g/WCAG/TradingView/Carbon/Pajamas), trialed in 12 exhibits + a 7-exhibit revision pass — all approved. Key locks: research-tuned skeleton (whispering gridlines, heavier baseline, unit-once captions, chart surface lighter than page) · series system with **Verdigris as dark chart hero — JCJ Teal banned as chart hero ("neon")** and a visibility floor on context lines · Realized/Terracotta pos-neg with mandatory sign+value labels · marker grammar (solid event / dashed NOW / everything labelled) · 8% threshold bands · **right-stacked legends (owner override of the direct-labeling research recommendation, for readability)** · 1.5px etched outline icon system with custody motifs replacing retired padlocks · diagrams with the **arrows-are-structure law** (ink-tone connectors, never accent — "must not overshadow text") and the **muted-has-a-floor law** (explainer text never below the strengthened meta register) · deepened anchor panels · sparklines · diverging ramps **flipped to stressed-left → calm-right (owner reading-direction law)** with raw red-green banned by judged exhibit · status glyph ladder (colour+shape+symbol, persistence scales with severity) extending the locked status system · **motion law: no load animation (web research struck the draw-in — transient false trends), state-change transitions only (150–250ms staged), reduced-motion = instant.**
**By:** Owner, via Trials B and B2. **Why:** All five layers — dark palette, light palette, typography/emphasis, components/layouts, charts/icons/motion — are now judged, evidence-backed, and registered. Next: Guide v3 consolidation → DESIGN-CONTRACT token cut → P0-S01 re-port → Phase 0 build, preview-first.

### 2026-06-05 · Guide v3.1 — emotion layer + corpus fragments locked; relevance cut applied
**Decision:** Guide bumped to **v3.1.0** with the **emotion layer**: every combo carries an emotional-intent line (evokes/calms/avoid-for), the 198 corpus transition pairings are web-translated into scroll-adjacency grammar (46 allowed / 8 conditional / 28 forbidden), gradient zones become **scroll-zone anatomy** (Entry/Transition/Resolution, CTA-at-resolution), and 7 new laws enter Ch5 (No-Risk-Chain · Scarcity ≁ Legal · No-running-text class · Disclaimer floor · Single-line footer · One-CTA + summary exception · CTA-at-resolution) plus the bullet-label type role and weight↔role ladder. Tokens (standalone + embedded) gain `emotionLayer` machine-readable. Backed by two new committed research artifacts: `COLOUR-EMOTION-REGISTER.md` (verbatim, 33 colours — the content itself, per owner critique) and `v31-fragments.md` (47 verbatim pulls). **Relevance cut (owner):** print/CMYK/platform-scores, pairwise matrices/triads, regional registers, deck pacing, corpus governance + font-stack records PARKED in research, excluded from the guide; **RTL/Arabic flag EXCLUDED entirely — no market or language differentiation.** **Conflict rulings (owner):** one-CTA keeps the section-summary exception · disclaimer floor 12px web (9pt = print-only) · Teal full-background corpus permission overruled by verdict ban (OVERRIDE). Gold↔ETF adjacency resolved to forbidden (stricter verdict).
**By:** Owner (relevance cut + 3 rulings + "begin") · Opus orchestration · 3 Opus agents (register capture · fragments pulls · application derivation) · 1 QC agent (5/5 PASS incl. both JSON payloads).
**Why:** The coverage-gap sweep's mined content was being consumed invisibly; the register makes it visible and citable, the guide fold-in makes it *applicable* — verdict-filtered, current-hex-only, slide-grammar translated to scroll-grammar. The emotion layer is the colour backbone the Meaning-Layer spec builds on.

### 2026-06-05 · The Comfort Ceiling law (v3.1.1) — the system's first generalized contrast ceiling
**Decision:** New LAW: every element targets a salience **band**, never a maximum — ambient/structural elements (body text, surfaces, lifts, connectors, gridlines) sit inside the band; full salience is **spent** on meaning-carriers. Light body band 8–12:1; token fix `--body: #3A434C` (10.1:1 on white — light finally gets the ink→body two-step dark always had; ink #2B2F34 = headings/labels only). Theme-conditional; cumulative across scroll. Applied to Guide (CSS + Ch1 + Ch5 law row) and both token files; bumped to **v3.1.1**. White-card and ink verdicts untouched.
**By:** Owner (light-mode re-read: "very deep heavy black on a pure white surface… disruptive and distracting") → Opus diagnosis (light `--body` was a copy of `--ink`; the system had floors but zero ceilings) → owner-ordered verdict-evidence sweep (1 Opus agent, full trial record): **17 excess + 10 deficiency verdicts; hypothesis SUPPORTED with no counter-evidence** — the owner had been judging a salience band all along (B2-R1 both-edges, light ban #4 midpoint-lock, B9 causal statement "fill too bright… value hard to read"). Owner: "good work, do it carefully and with logic."
**Why:** Excess verdicts were codified as scoped bans (chart-hero, arrows, gridline-whisper, Saturation-Footprint) while deficiency got generalized laws — the asymmetry let max-on-max body text ship unflagged. The ceiling generalizes what the verdict record already established: one element's excess creates another's deficiency.

### 2026-06-05 · Guide self-compliance audit + fixes (v3.1.2) — the guide now obeys all its own laws
**Decision:** Full self-audit (38 laws × both themes, computed contrast): 9 violations, 2 law-wording conflicts, 4 improvements found; all fixed. Key fixes: light LAW/RANGE chip overrides (were 1.86–2.9:1 — the rule-class system itself unreadable in light) · `prefers-reduced-motion` media block + `:focus-visible` + keyboard paths and roles on nav/law-anchors + taller tap targets (the guide demanded focus states of others while shipping none) · theme-conditional specimen tokens (`--st-*`, `--pos/neg-*`, `--annotation-tx`, `--alert/clay/risk-label`) replacing hardcoded dark values that failed in light (pos-green was 2.07:1 on white) — the "live tokens, not illustrations" promise now holds under theme switch · "Join →" footer chip routed through `.btn` (was white-on-Teal 2.38:1 in dark, the canonical white-text-ban case) · `.bound`/light-`.met` faint→muted (muted-floor) · `.rt .no`/matrix marks to 700 + theme-safe tones (Weight Law) · Always/Never labs recoloured to ink + ✓/✗ glyphs (colour+shape). **Two law amendments** (wording, not rendering): Caps-confinement now enumerates its real licences (micro-label · kicker · table headers — the old text outlawed the sanctioned kicker); Blue-ink gate scoped to running/body text (bold micro-chips ≥4.5:1 exempt). Footer trimmed to one short line. Versions: guide + both token payloads → **3.1.2**.
**By:** Owner ("read through the laws and see the HTML… make a list" → "make all the improvements") · Opus audit agent (findings) · Opus orchestrator (fixes).
**Why:** A guide that violates its own laws teaches violations — every specimen is something an agent will imitate. The audit also proved the queued Interaction & States trial is overdue: focus states were the largest class of self-contradiction.

### 2026-06-06 · DESIGN-CONTRACT v2.0 cut + P0-S01 re-port (token layer live)
**Decision:** `DESIGN-CONTRACT.md` rewritten as **v2.0**, pinned from `vegaready-tokens-v3.1.json` (v3.1.2) — the 2026-06-03 Themeable-Demo contract (Muted Petrol/Terracotta light-primary) is retired. The contract is now the build-facing cut of all five locked layers: dark-primary WB5 Deep Desk + L1 Soft White Desk token tables (the lint surface), Comfort Ceiling + Weight Law + colour-role principles, status/chart/type registers, measured budgets, locked component registries + product components, and a legacy-alias map. **P0-S01 re-ported:** `src/styles/global.css` rebuilt on the contract — canonical tokens in `:root` (dark default), full `[data-theme="light"]` override, fonts swapped to Playfair/Fraunces/Lora (JetBrains Mono kept as flagged legacy register, 408 usages pending type-migration), all legacy token names (`--cyan`, `--bg`, `--text`…) kept as aliases with values remapped so the 1,800+ existing `var()` consumers inherit the locked palette without component edits; contrast-graded line-height via `--lh-body`; −15% hover law; `:focus-visible`; `prefers-reduced-motion` block. White-on-Teal CTA ban honoured (`--cta-text` = canvas-ink in dark, white only on light Teal Blue).
**By:** Cowork Opus (orchestrator/sole writer), per the locked order: Guide v3 consolidation → contract cut → P0-S01 re-port → Phase 0 build, preview-first.
**Why:** Until this cut, the contract still described the retired light-first system — specialists reading `/_program/` would have built against dead tokens. The alias remap makes the new palette visible site-wide in one step while keeping the migration honest (no new code may use legacy names).
